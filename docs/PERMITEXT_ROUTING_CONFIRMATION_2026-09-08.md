# Research routing repairs and visible conditions — September 8, 2026

The preceding live package failed MC-02 and PC-01 despite assembling their governing text. MC-02 acquired a Zoning review merely because the Mechanical Code question described a zoning lot. PC-01 made collateral occupant-load and fixture-count sources mandatory even though the question stipulated both premises and asked about separate-sex facilities. The semantic verifier rejected those collateral citations, then deterministic coverage rejected their removal.

The corpus router now distinguishes contextual intake-lot terminology from an independent Zoning request. Explicit ZR references, district/use questions, and lot-definition/formation questions retain their existing Zoning scope. Evidence discovery now routes separate-facilities questions to PC 403.2 and 403.3. When occupant load and fixture-count sufficiency are explicitly stipulated, the broad calculation routes do not become mandatory; a request to verify those premises or an exact calculation citation retains that coverage. Existing semantic and citation checks remain in force.

The main narrative must contain material substitution conditions; expandable supporting points and limitations cannot compensate for their omission. The verifier also recognizes when a known combined total establishes a threshold result for every possible subgroup. English curly quotes now survive sanitization, preserving Markdown emphasis.

## No-cost evidence

Actual source-assembly regressions cover the failed café question, a different population/wording, preserved exact BC 1004.1 coverage, and an explicit request to verify the counts. Corpus tests cover the failed intake question, a changed lot width/separation, standalone Zoning questions, and mixed Mechanical Code/Zoning requests. The original technical-scope and complete-source tests pass. The all-60 question-only retrieval audit retains 59 exact-reference matches, no empty results and no network/provider calls. Its remaining missing exact reference is the broad MC 403 grouping in MC-01; this audit is not a generated-answer grade.

The first broader test run caught added instruction size crossing the existing cumulative request cap in a ramp fixture. Wording was shortened without changing the cap; both ramp request-envelope cases now fit, including the settled-web plus complete-answer bound of $0.499385 against $0.50. These are synthetic reservation checks, not measured live costs.

## Bounded live package

The new runner is `permitext-sync-server/scripts/run-research-owner-routing-confirmation-20260908.mjs`. It defaults to a no-network preflight on the exact committed runtime, verifies governing source completeness and both repaired routing decisions, and uses a permanent result file to prevent replay. The planned order is MC-02, PC-01, PC-03, with one attempt each and the existing bounded internal revision policy. No expected answers enter the answering request.

All ten previous packages are settled. Their conservative total is $6.336181; estimated usage including cache-write premiums is $3.18788911. This package is capped at $0.80 conservative aggregate and $0.50 per complete turn, shrinking before each turn to the remaining package allowance. Cumulative exposure therefore cannot exceed $7.136181, within the owner's $8 authorization. Failed operations count; pending or missing accounting stops further work. No other paid package runs concurrently. This file records preparation; live findings will be appended after execution.

This work is local backend verification. No deployment or phone testing is included.

## First live attempt and additional repair

The first package at `521acce77` stopped after MC-02 in 24.898 seconds, with three dispatched provider calls. PC-01 and PC-03 were not attempted. Corpus selection was repaired, but evidence discovery independently marked the contextual zoning-lot phrase as an outside-library authority request. That caused a 10.212-second web request and inserted a collateral Zoning starting-point paragraph. The verifier rejected that paragraph and correctly required the omitted Group R-3 exception. The $0.50 per-turn spending guard rejected the planned revision before dispatch. No answer was saved or user turn charged.

The raw stopped result remains `permitext-sync-server/evals/results/research-owner-live-routing-confirmation-2026-09-08.json`; its runner is consumed. Three provider calls settled with zero pending requests. Token telemetry is $0.039255, cache-write premium $0.00779725, and two web-search tool calls add $0.02: estimated package usage **$0.06705225**. Conservative cost is **$0.121497**. Across eleven packages, cumulative estimated usage is **$3.25494136**, conservative cost **$6.457678**, with 51 case-turn attempts and 110 provider calls. These are usage estimates and bounds, not account-balance evidence.

The additional repair shares the same contextual-lot normalization between corpus routing and outside-authority discovery. The intake-location topic now makes complete MC 401.4 coverage mandatory, including its Group R-3 exception. An actual assembly/source-policy regression proves no automatic web request for the intake question, continued web eligibility for explicitly requested DOB guidance, and continued discovery of independently requested Zoning definitions. No verifier issue is bypassed. Current publisher text confirms the [ordinary intake rule and Group R-3 exception](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-193192), the [separate-facilities thresholds](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-161468), and the [fountain substitution conditions](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-161591).

The separate v2 runner accounts for the stopped package and keeps the same three-case order, $0.80 aggregate maximum and $0.50 complete-turn limit. Its maximum cumulative exposure is $7.257678. Its stronger no-network preflight requires the MC 401.4 exception and checks the actual automatic-web decision. The earlier preflight's fixture-table completeness assertion was narrowed to complete PC 403.2/403.3, while still requiring PC 403.1 to remain discoverable context. The original source-review artifact is unchanged. The active retrieval dataset version was aligned with the implementation; the twenty draft retrieval cases pass without paid calls.

## Completed v2 confirmation at `df4fce13d`

All three questions delivered answers, each with one draft and one successful semantic verification. There were no web requests or revisions. The immutable result and exact-source preflight are `permitext-sync-server/evals/results/research-owner-live-routing-confirmation-v2-2026-09-08.json` and `research-owner-routing-confirmation-v2-preflight-2026-09-08.json` in the same directory. Both runners in this report are now consumed and must not be replayed.

| Case | Result | Full request time | Main-answer words | Manual review |
| --- | --- | ---: | ---: | --- |
| MC-02 | Delivered | 18.354 s | 99 | Correct ordinary 10-foot rule, six-foot failure, inapplicable narrow-lot alternative, and express Group R-3 exception. The prior unnecessary web search and Zoning paragraph are gone. A collateral contaminant-distance scope note remains but does not change the result. |
| PC-01 | Delivered | 14.871 s | 125 | Correct combined employee/public 30-or-fewer exception applied to 28 people. Preserves the stipulated fixture count and relevant single-user/all-sex treatment. Prior 28.969-second failed correction is resolved. |
| PC-03 | Delivered; presentation gap remains | 18.306 s | 134 | Correct rejection of four replacements, 50% calculation and two-plus-two alternative. The main answer mentions a 10-inch container only while describing the retained drinking fountains; it still should explicitly apply that criterion to the dedicated replacement fixtures under PC 410.3. The small-occupancy exemption remains unnecessary. |

The latest PC-03 verification passed despite the actor-specific completeness gap, so delivery is not a full quality pass. Its prior 18.080-second request and current 18.306-second request do not establish a speed improvement. These single observations are not p50/p90 latency or full-service cost estimates.

The café source package fell from 23,065 to 16,191 characters, with PC 403.2/403.3 mandatory and collateral calculation sources no longer mandatory. The all-60 retrieval audit remains 59 exact-reference matches with no empty source packages and no external calls; it is retained in `research-owner-routing-retrieval-2026-09-08.json`. Main `npm run check` and smoke passed, and the full Research regression suite passed again after the additional intake/web-source repair. The active retrieval-version mismatch exposed by the broader check was corrected before these results were recorded.

Six provider calls settled with zero pending requests. Token telemetry is $0.139816, cache-write premium $0.02733275, and web-tool cost zero: estimated v2 package usage **$0.16714875**. Conservative package cost is **$0.322812**. Across twelve packages, cumulative estimated usage is **$3.42209011**, conservative cost **$6.780490**, and unused conservative authorization **$1.219510**. There are 54 case-turn attempts and 116 provider calls. Of the additional 60 cases, 28 distinct IDs now have delivered answers on their latest attempt; 32 remain without live generated-answer evidence. Known presentation/qualification defects remain open in some delivered answers. This work does not narrow the original 110-question scope or establish commercial readiness.

Next: scope the stipulated-fountain substitution question to its material rules so irrelevant applicability checks do not crowd out the replacement-fixture conditions; verify that conditions attach to the correct object in the visible answer; continue representative untested cases within the remaining authorization. The prior CC-04 benchmark amendment, original-case coverage gaps and source-refresh limitations remain open as recorded in the earlier reports.
