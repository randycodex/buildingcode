# Technical answer expansion — September 8, 2026

This package expands generated-answer coverage to five previously untested owner cases after the official-document and revision-verification repairs. It asks MC-05, GAP-04, PC-03, PC-08 and MC-10 once each through isolated local HTTP Research conversations with actual provider responses. Only the supplied question enters the answering path; reference answers and evaluator source lists are withheld.

The no-network preflight recovered each complete governing source. The current source package sizes range from 8,512 to 21,166 characters, with 14–17 sources per question. Source completeness is not an answer-quality grade. The main check and smoke suite passed at the preceding runtime commit `d8d88001a`; this package does not change the answering implementation or deployed configuration.

The source review uses the corrected owner key and official publisher text refreshed September 8:

- MC-05: [MC 405.2](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-193345), including the control requirement for each air distribution system.
- GAP-04: [AC 28-105.4](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-156291), preserving the express city-agency scope and distinguishing unsupplied state/federal authority.
- PC-03: [PC 410.3](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-161591), fountain substitution limit and qualification. The direct publisher fetch failed during refresh; the current indexed publisher passage and [DOB's 2022 chapter PDF](https://www.nyc.gov/assets/buildings/codes-pdf/cons_codes_2022/2022PC_Chapter4_FixturesWBwm.pdf) corroborate the operative limit.
- PC-08: [PC 419.2](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-161687), the per-room substitution limit.
- MC-10: [MC 501.3.1](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-193376), distinguishing openings serving the same and adjoining dwelling units.

## Budget and execution

Seven prior paid packages have a settled conservative bound of $3.492894 and zero pending provider requests. Five $0.85 turn caps add an absolute $4.25 ceiling, giving a combined maximum of **$7.742894** within the owner's $8 authorization. The package also enforces a $4.30 aggregate guard, and preserves its result with exclusive file creation before dispatch. Default execution is a no-network preflight. There is no separate paid judge, manual retry, or deployment. Existing bounded in-turn verification/repair policy and model routing apply.

The preceding usage estimate is $1.74661036. New cost and quality findings will be recorded from the completed operations and actual answers. A failed or source-incomplete answer will remain a failure rather than being counted as a pass based on internal verification.

## Live findings at `68be6ecaa`

The immutable result is `permitext-sync-server/evals/results/research-owner-live-technical-expansion-2026-09-08.json`. All five isolated conversations finished, with 11 provider calls and zero pending requests. Four main answers were supported on manual comparison with the official provisions above; one failed before delivery. Internal verifier acceptance alone is not the manual grade.

| Case | Outcome | Full local request time | Words | Review |
| --- | --- | ---: | ---: | --- |
| MC-05 | Delivered; main rule correct | 14.543 s | 174 | Correct per-system manual control and disconnect exclusion, but duplicated all rules in an unnecessary table and added replacement-design caveats. |
| GAP-04 | Failed, no answer/turn charge | 24.315 s | — | Both drafts cited the enacted city-agency rule, but lexical overlap with official pages triggered two deterministic attribution rejections before semantic review. The drafts also included an unnecessary DEC excavation example. |
| PC-03 | Delivered; main rule/calculation correct | 22.402 s | 220 | Four fillers cannot replace all four fountains; 50% means at most two. Qualification was preserved, but unrelated exceptions and repeated design-review notes made the answer too long. |
| PC-08 | Delivered; main rule/calculation correct | 5.067 s | 74 | Correct per-room 50% substitution, maximum two urinals for four required water closets. Presentation was appropriately concise. |
| MC-10 | Delivered; main rule/calculation correct | 10.230 s | 126 | Correct three-foot adjoining-unit requirement and two-foot failure. Could more directly explain that the two-foot allowance concerns the same unit and trim the closing checklist. |

These five samples do not establish population latency or commercial reliability. Additional-60 coverage is now 16 distinct live case IDs, not 60 generated-answer passes. Across all eight paid packages there have been 34 case-turn attempts and 70 provider calls.

Token telemetry is $0.232399. Current standard cache-write premiums add $0.04458845, and the three actual web-search calls add $0.03, giving an estimated **$0.30698745** for this package and **$2.05359781 cumulative**. The settled conservative operation ledger is $0.590934 for this package and **$4.083828 cumulative**, with zero pending requests. These are usage estimates/bounds, not an invoice or confirmed remaining account balance.

## Repair scope

A simple yes/no question containing “requirement” was classified as a requirements-table request when at least four sources were retrieved. The presentation contract now selects concise direct paragraphs for that question form, preserving open-ended requirements and explicit comparisons. Rules and calculations must not be repeated in multiple formats.

The lexical web-attribution heuristic remains conservative by default. Only the general-answer branch that already runs semantic verification may defer wording-overlap flags; its verifier receives exact flagged point indexes and web claim/source bindings alongside the enacted text. Explicit guidance misattribution, invalid bindings, missing requested documents and required source disclosures remain hard failures. Mock/deterministic-only paths do not receive this deferral. The real GAP-04 outputs are replayed with explicit accepting/rejecting verifier doubles to prove the save/charge gate; that replay is not a live legal-correctness result.

The new confirmation runner targets MC-05, GAP-04 and PC-03 once each. It verifies settlement of all eight prior packages and their exact file hashes, preserves question-only answering input, and uses exclusive result creation. Three $0.85 caps give a $2.55 absolute package ceiling and **$6.633828** maximum cumulative bound; an additional $2.60 aggregate guard applies. Its default preflight passes without provider calls. No production configuration is changed.

Validation completed: `npm run test:research-chat`, `npm run check`, `npm run smoke`, and the new confirmation runner's no-network preflight passed. The initial broad check exposed a missing dependency in the request-builder test harness and a tight ramp request envelope; the harness was corrected and presentation instructions shortened without raising the spending cap. Both ramp envelopes and the full suites subsequently passed. The new confirmation still requires live execution and manual source review.
