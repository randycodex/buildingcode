# PERF-11 acceptance audit

**Final status: complete locally for released web surfaces.** Implementation commits include `e05e9a551`, `7ffd9723d`, `4bb3b27ae` and the local static-serving correction `041ae9b94`. All eight priority-plan requirements have supporting evidence below. No production deployment or physical-device latency claim is made.

1. **Render entry points — verified locally.** Full and utility rendering mount shells independently. Reader/result navigation waits for its target. Saved Project-transition readiness has a follow-up fix and passing actual-caller regression. Continuity, startup restoration and workspace contracts pass; remote history deliberately does not steer the active workspace.
2. **Ordered independent publication — supported.** Coordinator contracts and controlled delayed-sync browser checks establish public publication and retained nodes. The reproduced internal chapter-normalization restart is corrected; pending/ready and explicit-navigation regressions pass.
3. **Verified cached continuity — verified locally.** Provenance contracts distinguish completed verification, permitted transport fallback and denial. Rendered fresh-load 503 recovery with a real installed 578-chapter library restores six panes through permitted-offline gates. Continuity review confirms remote history does not change the active workspace or columns; workspace/continuity/startup/scroll contracts pass.
4. **Private access — verified locally.** Browser sync 403/retry and actual sign-out pass; old gate references deny access after sign-out. Rendered A-to-B via a native cross-tab storage event now passes with B sync held and 130 samples excluding A private content. Same-user session replacement also passes via a native storage event with new sync held, private nodes removed, and clean recovery. Shared viewer read-only and revoked-membership denial pass HTTP checks; the released UI disables firm collaboration, so rendered shared Notebook testing is not applicable without enabling an unreleased surface.
5. **Isolated failure/retry — verified locally.** Actual Retry and coordinator tests pass. Browser sync failure leaves public panes usable. Rendered Report HTTP503 and its actual Retry recover with five neighboring panes and Notebook editor preserved.
6. **Editor and layout preservation — verified locally.** Mixed browser evidence retains Notebook draft/focus/selection when Report finishes; public panes retain Search focus/selection after sync. The broad capability-based identity defect is fixed; pane-specific capability regression tests preserve dirty editors on unrelated changes. Relevant permission revocation passes orchestration checks; real pointer resizing to 700px persists across Report close/reopen with all five neighboring nodes and widths unchanged. Real pointer reorder of Search after Reader retains all six pane nodes; Report close/reopen preserves the resulting relative order.
7. **Request reuse — verified locally.** Project transition payloads validate account/session/workspace/project. Scoped initial sharing now reduces observed early foundation requests from two to one in the six-pane fixture; a later five-second request remains to attribute. Do not coalesce intentional mutations or refreshes against stale inputs.
8. **Complete fixture — verified locally.** Synthetic multi-pane evidence covers delayed Report and delayed/denied sync. The combined evidence covers access, failure, installed sync fallback, editor state, resize and drag order.

Evidence: `PERF_11_MIXED_BROWSER_EVIDENCE_2026-09-23.json`, `PERF_11_PUBLIC_STARTUP_BROWSER_EVIDENCE_2026-09-23.json`, expanded `test:workspace-access`, `test:workspace-pane-hydration`, offline and smoke suites. These are local functional checks; they do not establish production or physical-device latency.

Follow-up findings: the Saved target-readiness correction passes its actual-caller test and the full access suite. Source inspection confirms restored Saved and Notebook both request the same Project foundation when no transition hub exists; initial presentation-scoped sharing now covers non-overlapping initial consumers as well as in-flight calls, with mutation invalidation and lease cleanup tests. Distinct Report endpoints and intentional later Saved refreshes are excluded from this finding.
