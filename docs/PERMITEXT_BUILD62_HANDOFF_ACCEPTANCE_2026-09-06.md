# Build 62 retained Research handoff acceptance — September 6, 2026

Status: **Retained Note review, current web Note navigation and web/native Report
presentation passed; the owner confirmed the Chrome Production PDF download.
Temporary Pro is revoked, and the phone retains the Free test session at the
owner's request.** The in-app browser download remains unaccepted. This continues
the original production-readiness audit and the
[September 6 repairs](./PERMITEXT_RESEARCH_HANDOFF_REPAIRS_2026-09-06.md).
It does not close the wider Beta 1 acceptance gates.

## Candidate and scope

- Production source: `e60ca415fe8b7b60be65449b7ef49baccc82eec3`, release
  `e60ca415fe8b`; deployment `dpl_5rp8vnZ9yZ6XBRWbFHofaD7jQqo9`.
- Physical candidate: Permitext 1.0 (62), independently read from its Account
  footer. Its publication, archive and source binding are in the repair record.
- Operator: Codex, with owner-assisted sign-in and physical Note scrolling.
- The owner approved temporary no-charge Pro for the retained designated test
  account, existing Note/Report checks, subsequent revocation and usual-account
  restoration. After this check, the owner asked to keep using the test account
  during the ongoing audit instead of repeatedly restoring the usual account.
  No additional Research generation, paid retry, purchase, account
  deletion, deployment or TestFlight upload was performed.
- The usual phone account initially showed Lifetime Pro Active, 98 included
  turns and Synced, with existing Projects and its saved Building Code passage.
  This observation was recorded at `2026-09-06T15:37:53.587628+00:00`.
- The exact designated test identity was independently checked before its grant
  at `2026-09-06T15:37:54.401Z`. Both test clients subsequently showed Pro with
  99 included turns. Production exports are scoped by the exact account and
  retained privately; raw identities and credentials are excluded here.

## Verified behavior

The browser resumed cached client `20260905-account-verification-ui-v43`, which
still reproduced the hidden-answer behavior. One ordinary reload loaded
`20260906-research-handoff-v48`. On that current client, an actual Note-reference
click revealed the existing answer. After closing it, Enter on the same Note
reference reopened it. The browser error log was empty for these checks.
The server release footer alone was insufficient to establish the loaded client.

The physical Note retained its preparer/synthetic-use statement, 2014 source
basis, conditional slope exceptions, unconfirmed applicability, upper-floor
assumption, cellar-only sprinkler qualification and final verification checklist.
The owner scrolled the lower fields after Mirroring could not move that Note
screen. No Note edit or Save was performed. This establishes readable retained
fields; it is not a new native citation-navigation acceptance result.

The web Note's **Update in Report** action created draft revision 3 from
revision 2, preserving its independent authored snapshot while adding the
missing boundary before `Research:`. The Note itself remains version 2.
**Export Report** created Report version 2. The phone received that version,
and **Export & Save iOS PDF** generated and stored its native PDF; the iOS share
sheet showed the resulting 49 KB document. No share recipient was selected.

## Exact exported artifacts

Both new PDFs reference manifest `c99b08be-2fe8-4e39-995b-f2a638a04d78`, Report
version 2, manifest SHA-256
`ba5d74a8cd4d17c06f84077593fe0f4af0296fcb08d5315a458aab31df532112`.

| Export | Bytes | Pages | SHA-256 |
| --- | ---: | ---: | --- |
| Production web PDF | 40,172 | 5 | `3b09d567c58cf9f75ccbafcd5205b7865341afc828f83742c6168f381d47210e` |
| Physical build-62 iOS PDF | 49,150 | 4 | `00731ddfd9b799f9985b2fd8f86d94638ab27ba5db521efd668d2691c621e5dd` |

Each exact private stored file was independently read and matched its descriptor
size/hash. Every rendered page was visually inspected. Both exports distinguish
the Project default (2022) from the included Research basis (2014), render the
two 2014 BC citations as readable code/section/title/edition labels, preserve
assumptions and partial coverage, show the workflow arrows, and remove literal
Research emphasis markers. The new Note snapshot has the corrected reference
boundary. No clipping, overlap or trailing hash-only page was observed.

The old immutable answer's unrelated program-document limitation remains in
the new Reports because they intentionally include that saved answer. Its
qualified authored Note/introduction explicitly exclude the unrelated Zoning
footer as evidence. Presentation acceptance does not establish that the old
answer has been semantically corrected or authorize another paid generation.

## Record integrity

The independent comparison recorded at `2026-09-06T15:59:14.935289+00:00`
confirms:

- Research answers, conversations, operations and usage are byte-equivalent
  under canonical JSON comparison: one answer, one conversation, three
  operations and one prior charged turn remain.
- All saved/Project mutations and every prior artifact except the explicitly
  updated Report draft are unchanged, including all three Notes, both original
  PDFs and the original Report manifest.
- Exactly three artifacts were added: the new manifest, web PDF and iOS PDF.
  The Report draft alone advanced from revision 2 to 3.

## Browser download acceptance

The in-app browser did not deliver a download event for its visible **Download
Web PDF** action. The owner independently clicked it and reported that nothing
happened. This download path therefore remains unaccepted in that browser.

An isolated loopback fixture used the unchanged current download function and
the exact new synthetic web PDF, with no Production authentication or external
requests. Both browsers completed the 40,172-byte local PDF response without a
JavaScript error. Chrome's native download panel independently showed the
diagnostic file as **39.2 KB · Done**. The download-event API timed out even for
that completed Chrome download, so its absence alone is not a reliable failure
signal. This control supports a browser-specific cause. The local server and
probe tabs were closed.

Chrome's usual owner account was verified as Lifetime Pro, 98 included turns and
Synced, then signed out for the scoped test-account control. One reload loaded
the current `v48` client. After owner-assisted email verification, the designated
test identity, Pro/99-turn state and Synced status were verified. The existing
Project's Report version 2 was opened and its newest **Download Web PDF** action
was clicked. No application error appeared, and the owner explicitly confirmed,
**"The PDF downloaded."** Hosted Chrome download acceptance is owner-confirmed;
the downloaded local file was not independently hashed. The stored PDF was
independently verified as described above.

## Completed cleanup and retained session

The scoped temporary Pro grant was revoked and independently verified at
`2026-09-06T16:15:33.094Z`. Both browser clients reloaded to Free and were signed
out through the application; each ended with a **Sign in** control. The in-app
browser returned to its initial signed-out state.

The phone showed **Free Active**, **Billing: None**, **Synced** and the designated
test email. The owner then asked to keep the test account available during the
ongoing audit rather than repeatedly restoring the main account. Its native
session is intentionally retained. Usual-account restoration is deferred until
the audit ends or the owner requests it; no restoration action is pending for
this checkpoint. Future checks can reuse the test session, with any additional
Pro access governed by its own authorized scope.

The final independent export at `2026-09-06T16:17:56.621Z` confirms a null
entitlement, a retained session, one prior Research usage record and zero
purchase claims. Its export SHA-256 is
`e1f0e237a5386fd1b84da550a92da212ef02c0b2ce0de5cf62cac216c07c7cd2`.
The final retained-record comparison also matches the post-native-export
Research, artifact, revision and Project-link hashes.

## Remaining scope

Broader Project reassignment with different facts, remaining original audit
acceptance cases, final selected-release binding and owner release approval
retain their separate status. No public App Store submission is authorized.

Private evidence: `/private/tmp/permitext-build62-handoff-acceptance-20260906/`,
including the scoped approval/grant/revocation receipts, baseline/after-export/final summaries,
record-integrity comparison, exact PDFs, extracted text and rendered pages.
The private folder contains sensitive operator material and must not be staged.
