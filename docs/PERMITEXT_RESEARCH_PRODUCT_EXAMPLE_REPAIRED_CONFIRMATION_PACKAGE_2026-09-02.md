# Permitext Research repaired product-example live-confirmation package

Date: September 2, 2026

Status: **LOCKED — NO NETWORK OR MODEL CALL AUTHORIZED**

## Purpose

This is a distinct post-repair confirmation package for the owner's seven real
Permitext examples and nine ordered turns. It does not reopen, replace, or alter
the consumed package `41b4f2612b1c982fca60de6400fe802aded5a193`, its one
permitted run, or its `$1.023256` settled spend.

The repaired implementation is fixed at
`c1ea51e3b55aaa15be2639b0cdf14c3461b35b2e`. The exact package commit is the
later commit containing this locked package. That package commit must be named
verbatim in a new owner authorization before any provider access.

## Exact live scope

- Seven conversations and nine ordered turns, in the frozen order.
- One repetition only.
- Maximum cumulative API spend: `$2`.
- No separate paid judge.
- Automatic enacted-corpus retrieval and conversation continuity enabled.
- Official web support enabled, restricted to the configured official domains.
- A permanent one-attempt lock is created before the local evaluation server or
  any provider request.
- An isolated temporary local store is used; Production databases and storage
  variables are cleared.
- Public Zoning Research stays disabled. The C4-4D example uses only the
  existing diagnostic path for this exact locked evaluation.

## What changed from the partial live result

- 2022 Appendix P can use its exact title-only `Reserved` evidence instead of
  rejecting before provider access.
- C4-4D/R8A retrieval retains all four controlling provisions, and its second
  turn preserves the active topic while requiring a short table-free paragraph.
- The 2014 vision-lite answer must show that `100 square inches ÷ 144 = 0.694
  square feet` is a derived unit conversion.
- The OMH/ADA turn seeds the official OMH regulations index and official ADA
  site into attributable web discovery. A nonofficial candidate URL is rejected
  before the provider request. Official supporting material remains visibly
  separate from enacted Permitext code evidence.

## Deterministic result review

Every completed answer still requires owner review. The retained automatic
review checks the following before reporting a turn as passing:

- required enacted references for the first-turn code examples;
- no forbidden 2022 vision-lite reference in the 2014 conversation;
- Markdown tables for the ramp and full C4-4D comparison;
- a table-free C4-4D follow-up of no more than 140 words that retains the
  controlling C4-4D/R8A relationship;
- explicit `Reserved` status for Appendix P;
- explicit `2014` confirmation and the requested derived square-foot value;
- attributable `omh.ny.gov` and `ada.gov` sources for the external-authority
  example; and
- no OMH-style `every N` ratio unless the same ratio is present in an attributed
  supporting-source claim.

The live run is evidence for product behavior only. It is not a professional or
official code determination and does not authorize public Research, public
Zoning Research, pricing changes, merge, push, deployment, TestFlight, or
release.

## Locked artifacts

- Frozen cases:
  `permitext-sync-server/evals/research-product-example-cases.json`
- Runtime no-cost regression:
  `permitext-sync-server/tests/research-product-example-runtime-contract.mjs`
- Repaired no-cost preflight:
  `permitext-sync-server/evals/results/research-product-example-repaired-confirmation-no-cost-preflight.json`
- Preflight runner:
  `permitext-sync-server/scripts/preflight-research-product-example-repaired-confirmation.mjs`
- Locked authorization:
  `permitext-sync-server/evals/research-product-example-repaired-confirmation-paid-authorization.json`
- Authorization validator:
  `permitext-sync-server/evals/research-product-example-repaired-confirmation-paid-authorization.mjs`
- Live runner:
  `permitext-sync-server/scripts/run-research-product-example-repaired-confirmation.mjs`
- No-cost authorization guard:
  `permitext-sync-server/tests/research-product-example-repaired-confirmation-paid-authorization-contract.mjs`

## Authorization boundary

The committed authorization is intentionally locked: all active scope and
owner-decision fields are null, and `networkOrModelCallAuthorized` is false.
After the package commit exists, the only accepted sentence is:

`authorize exactly package commit <40-character-package-commit> for all 9 ordered turns in 7 conversations, one repetition, with a maximum cumulative API spend of $2.`

No earlier owner sentence, broader instruction to continue, or consumed package
authorization can unlock this package.
