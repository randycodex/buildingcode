# Research direct-rule accuracy repair

The two [retained live answers](../permitext-sync-server/evals/results/research-owner-live-zoning-small-confirmation-2026-09-08.json) exposed different gaps: ZR-01 omitted the reference key's particular-over-general principle; ZR-14 added a higher-building requirement without its qualification. The spacing source was also cut off within that qualification. Their original answers and grading remain intact.

## Implementation

Complete, canonically verified Zoning selections now receive their full allocation before supplemental discovery when the complete group fits the existing path budget. The spacing Reader selection retains all 5,301 characters, including the closing conditions, instead of stopping at 4,000. Its overall budget remains 8,000 characters. Partial, altered, unresolved, oversized and structured selections retain their existing boundaries. The storage-applicability excerpt policy remains separate and unchanged.

Source-derived answer obligations now check the construction principles, closest-point spacing, stated shortfall and rear-yard-equivalent exception. A higher-building comparison must be qualified if the answer introduces it. The checks accept different phrasing and allow the calculation in the main answer when the governing rule is source-bound. They are bounded deterministic checks, not a general semantic proof or professional review.

Replay initially exposed false positives in the new checks. The final implementation preserves the older qualified spacing answer and its main-text arithmetic, and accepts “give controlling effect to the text.” The later 21-answer retained cohort now preserves 15 previously accepted answers, rejects its five known failures, and separately records one newly enforced coverage gap: the omitted particular-over-general principle. Historical judge scores were not rewritten. The older 14-answer cohort preserves nine and rejects five under current obligations.

## Verification and bounded confirmation

The focused tests reject both recent retained defects and accept corrected, source-bound fixtures, including paraphrases, changed arithmetic, qualification negation and complete multiple selections. Research chat tests, both Zoning architecture suites, the 30-case architecture preflight and HTTP request-binding checks passed at no cost. The general `npm run check` was interrupted after the owner restricted further work to Research; it is not recorded as a full-suite pass.

The full authored-input source diagnostic covers all 110 cases, preserves all eight exact selected passages and all 47 authored pins, and leaves the existing missing-fact boundaries in place. The whole-section policy also restores the complete 8,595-character ZR 11-333 source in ZR-17 within that path's existing budget. The existing MC-01 exact-reference discrepancy remains; reference presence is not an answer-quality grade.

The new live confirmation is limited to ZR-14 and ZR-01, once each, with the existing routing and verification policy. Each complete turn is capped at $0.06; the aggregate evaluation ceiling is the remaining $0.135848 of the original $8 authorization. The production per-turn guard and separate aggregate evaluation guard both remain active. The latter reserves the configured base-rate bound before the model-specific guard, so a draft fitting its Luna bound alone does not establish that it can dispatch. A no-cost HTTP preflight checks the actual request shape under both guards. More concise obligation instructions preserve the full evidence and reduce that request overhead.

Before this confirmation, the 20-package ledger records $7.864152 conservative usage, 158 provider calls, zero pending requests, and 43 of 110 numbered cases attempted live. These are campaign accounting figures, not a provider invoice or verified account balance. The new package cannot replay an existing paid result; committed source, prior ledger hashes and an actual HTTP preflight must match before dispatch.

## Product direction and limits

This work supports dependable NYC research by improving completeness, qualifications and traceability. It does not establish representative live accuracy, response-time improvement, professional adoption or commercial readiness. Generalization to unfamiliar questions and representative latency measurements remain necessary.

The owner explicitly deferred project-workflow testing until requested. Continue Research answer, accuracy and speed work; do not start the research-to-project/Notebook/Report/reopen workflow test. No push, deployment, public eligibility change or phone test is included.
