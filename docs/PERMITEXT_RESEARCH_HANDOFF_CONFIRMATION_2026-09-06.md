# Research handoff confirmation — September 6, 2026

Status: PR #57 is published and its one approved live turn completed. The
2014-only source routing and selected-passage web boundary are verified in
Production. Full professional handoff acceptance remains **OPEN** because
answer-quality, Note navigation and Report presentation defects remain.
This continues the original 17 findings; it does not authorize public release.

## Publication and bounded confirmation

The owner approved PR #57 plus one unchanged synthetic ramp question on the
designated test account, capped at $0.50 cumulative per turn, with no subsequent
manual retry. The existing temporary-Pro grant/revocation authorization was
reused. This approval is consumed; no further generation occurred.

PR [#57](https://github.com/randycodex/buildingcode/pull/57) merged from reviewed
head `6a2627cc7ae72539928d13ae4e2a7bf9c24dbc04` at
`2026-09-06T03:36:47Z`, producing main commit
`4ed4b5b9f4eba77683af8aedb4f38fc3e0b4421e`. Git triggered Production deployment
`dpl_B8JvS8JLqUr7fSpuKTBZmGP2arNV`,
`https://permitext-sync-orugmme7o-randycodexs-projects-b72fc111.vercel.app`,
READY at `2026-09-06T03:39:32.216Z`. At 03:39:42.064Z both public origins'
`/release` and `/health` returned that exact SHA, configured commercial
authentication and PostgreSQL `normalized-v4`. `npm run verify:production`
passed. The scoped pre-submit error/fatal log scan returned no entries.
The preview was not promoted. No native runtime or TestFlight build changed.

A fresh scoped baseline matched the designated test identity, absent
entitlement, three saved items, two synthetic Projects, two prior Notes,
one empty conversation, two failed operations and zero usage/answers.
Temporary Pro activated at 03:38:05.146Z. The owner completed browser email
verification; the browser showed the test identity, 100 turns and the exact
new release. Project A's default remained 2022, while its description, four
qualified facts and selected/related 2014 sources were unchanged.

The retained question's Retry control submitted the one newly approved turn.
Request `9b8kc-1788666034135-ff9e207b729f`, operation
`c6d70110-ea92-4161-8194-45fa56981240`, completed on the new deployment in
33,126 ms. The in-flight export independently recorded the $0.50 reservation.

- One delivered answer, two persisted messages and one included test-account
  turn charged; the test allowance became 99. This was no purchase or charge
  to the owner's account.
- Three provider requests: initial Terra answer, Luna semantic verification
  and one built-in Terra revision. No web-support request or search occurred.
- Token-based provider estimate: **$0.068287**; conservative accounting:
  **$0.159382**. No provider request remains pending. These are application
  estimates, not invoice-reconciled costs.
- The searched corpus and both citations are exclusively the 2014 NYC
  Construction Codes, DOB consolidated archive. The registry version is
  `20260906-edition-qualified-citations-v4`; the 2022 Project default was not
  retrieved. The prior-edition applicability boundary is visible.
- The answer retains the assumed egress role, unconfirmed conversion/code
  applicability and cellar-only sprinkler coverage. The enacted slope rules
  and conditional exceptions match the supplied BC 1010.2/1028.11 passages.

The application's final verification status is `passed`, but this is **not a
clean professional-quality result**. Its first semantic verifier identified
an unrelated Zoning citation and an unsupported program-document requirement.
After the built-in revision, a deterministic post-repair gate accepted the
answer. The visible final answer still includes an unrelated Zoning
starting-point footer; its stored evidence limitations still require a
controlling program document before relying on a program-specific minimum,
although this question requests no such program. That limitation propagates
into the PDF. The saved answer and diagnostics now provide an exact input for
a no-provider reproduction; do not weaken attribution checks or run another
paid question to investigate it.

## Saved handoff and physical observations

Both web citation controls reopened the correct 2014 Reader destinations:
BC 1010.2 Slope (`41009495`) and BC 1028.11 Assembly aisle walking surfaces
(`41009715`). No current-edition Gates passage was substituted.

A separate synthetic Note, **AUDIT TEST - reviewed 2014 ramp slope - R1**,
explicitly identifies Codex as its preparer and disclaims any real property or
professional approval. It records the qualified review, excludes the unrelated
Zoning footer as evidence, and links the immutable Research answer. The Note
is `af894e09-0677-5853-a802-f4ac4272ebe0`, version 2. Its stored reference
matches the delivered answer ID.

The Note was promoted through Add to Report, producing an independent
user-authored paragraph snapshot. The saved Report draft
`35042b05-5650-4a47-b501-d806947e3871`, version 2, includes that snapshot,
qualified Project facts and a separately labeled AI-assisted answer. The
introduction identifies the synthetic acceptance purpose and limitations.
A full browser reload preserved the Note, reference, Report and history.
Independent exports confirmed identical answer and artifact records.

The Note's **Open Research** reference is not accepted: both Enter and a
pointer click produced no answer panel or error. The DOM retained the correct
answer ID; direct History reopening succeeded. This is a captured navigation
defect, not evidence that the referenced answer was lost.

The owner signed the physical phone into the designated test identity. Account
showed temporary Lifetime Pro, 99 included turns and Synced. Project A showed
one Notebook item, one Research item and one Report. The Note title, initial
review paragraphs and one linked reference reopened. Mirroring did not scroll
the lower Note fields, so their complete on-device review is not claimed.
The saved Research answer reopened. After the owner physically scrolled to
its source controls, both citation buttons independently opened exactly at
2014 BC 1010.2 and 1028.11 in the native Reader. This uses the previously
confirmed build-61 candidate; no new archive or upload occurred.

The reversible context check removed the conversation from Project A, then
assigned it back. Independent exports confirmed current `primaryProjectID`
changed to null and back, while the full immutable answer record, historical
messages, Note, Report draft, manifest and file descriptor remained equal.
No new Research operation occurred. A different Project with different facts
was not exercised, so that broader context case remains distinct.

## Export inspection and remaining defects

Export created manifest `e36f0cc1-2549-4a43-85d0-dc2bb29e9521`, version 1,
and generated web PDF `776e4d22-06b0-4eb7-b4fb-6f0f6869a92d`.
The private stored PDF is 12,539 bytes, five pages, SHA-256
`441ee7a8db941df5eccd1d1c7d2bb80e01506ed2b534af70f9f58af0ce1764a4`.
Every page was rendered and visually inspected. Qualified facts and distinct
Note/AI content are retained, but export acceptance fails these checks:

1. Cover metadata says **2022 New York City Construction Codes** without
   distinguishing that Project default from the included 2014 Research basis.
2. Citations on page 4 print internal section/source IDs rather than readable
   code, section and edition labels.
3. Unicode arrows in the introduction render incorrectly, Markdown emphasis
   markers print literally in Research prose, and the Note reference text is
   joined to its preceding sentence without separation.
4. The unrelated program-document limitation remains in the saved answer and
   PDF despite the earlier semantic verifier's objection.

The IAB Download Web PDF action returned neither a download event nor a local
file. The exact stored Blob was independently read using the scoped exported
file descriptor and matched its immutable hash. This verifies stored bytes;
it does not substitute for browser download acceptance.

After the owner opened the Report on the physical phone, its card showed four
items and the stored Web PDF. **Export & Save iOS PDF** completed on build 61,
opened the native share sheet, and added Stored iOS PDF to the Report card. The
share sheet was dismissed without sending the file elsewhere. An independent
export confirmed generated file `39aaaa2b-c0c5-460c-9198-0edde3daffc0`, created
at `2026-09-06T04:03:41.449Z`, using the same immutable manifest. Its private
bytes matched the descriptor: 47,224 bytes, four pages, SHA-256
`aea2a5207dc11c6fb1ed11f73971ea1c421a8e00e9ff0216b17c1cd9e248dd3a`.
All four pages were rendered; pages 1–2 were visually inspected before the
owner paused the session. They retain qualified facts, but page 1 repeats the
ambiguous 2022 cover edition and joined `review.Research:` text. Native arrows
render correctly. Pages 3–4 still need visual inspection; complete native PDF
presentation acceptance is not claimed.

## Cleanup and next work

The exact test account's temporary Pro grant was independently confirmed
revoked at `2026-09-06T04:04:47.341Z`. Browser and phone each showed Free before
sign-out. The browser test session is signed out. The phone now shows a blank
sign-in form; restoring the owner's usual Lifetime Pro session is **pending**.
The owner said the phone is unavailable and asked to continue tomorrow, so
phone interaction and acceptance work are paused. No account deletion occurred.
The final independent export at `2026-09-06T04:10:27.958Z` confirms no test
entitlement or active app session, one answer, one usage record and three total
operations. It retains three Notes, one Report draft, one manifest and both
generated PDFs; no further turn was issued.
The two original Notes and their Project are unchanged; new synthetic account
evidence is retained. The owner's plan, 98-turn balance and saved data must be
rechecked after normal sign-in, rather than inferred from the earlier session.

Resume with owner phone sign-in/restoration when available and visual inspection
of native PDF pages 3–4. Browser download and the lower native Note fields remain
separate acceptance limits. No additional paid generation is authorized.

Next work is a no-provider reproduction of the remaining verifier/limitation,
Note-reference navigation and Report edition/citation/formatting defects,
followed by focused repairs and rendered verification. Existing historical
answers and manifests must stay immutable. The bounded current routing result
does not close all Research quality gates, the original 17 findings, final
candidate binding or owner release approval.

Private evidence is under
`/private/tmp/permitext-research-selected-passage-confirmation-20260906/`:
`approval.json`, `production-verification.json`, `confirmation-result.json`,
`runtime-confirmation.jsonl`, scoped Project inspections, `handoff-verification.json`,
`pdf-review.json`, `final-summary.json`, and both `synthetic-handoff-web.pdf`
and `synthetic-handoff-ios.pdf` with their page renders.
Credentials, OTPs, raw account exports and real account identifiers are excluded
from this repository record.
