# Decision facts and bounded correction — 2026-09-08

The prior live laundry and water-heater answers gave supported direct conclusions but placed prospective design details in `missingFacts`. A shared generation/verifier instruction now limits that field to facts that can change or determine the requested result. It applies to positive, negative and non-numeric questions, while retaining applicability and exception facts and inputs needed for a requested design or calculation.

The no-cost replay exposed an important cost consequence: after the recorded laundry draft and first verification usage, a full-answer rewrite for this small field correction could not be reserved under the existing $0.50 turn cap. The water-heater rewrite could fit. Neither the cap nor the pricing ceilings was changed.

The verifier can now identify zero-based indices of entirely unnecessary `missingFacts` entries. A candidate correction may remove only those entries, only after a failed verification whose issues are all `unnecessary_qualification`. Mixed substantive issues, malformed/duplicate/out-of-range indices, passing responses and missing indices cannot activate the edit. Entries containing material applicability or exception facts must not be selected. A mixed necessary/optional entry requires a normal revision rather than deletion.

After the candidate edit, Research reapplies declared-fact protection and its ordinary evidence gates, then performs a new full semantic verification. Acceptance is never inferred from the first verifier's suggested edit. A failed final check prevents saving/charging the answer. Zoning retains its existing repair-eligibility and attempt limits. This path changes neither the narrative nor the supported rules, citations, other answer fields, or model routing. Saved metadata distinguishes a fact correction from regenerated answer prose.

Local contracts cover the production parser, invalid and mixed-issue inputs, immutability, exact preservation of the other answer fields, and four HTTP accept/reject replays of the two recorded cases. The HTTP replays use the recorded token usage instead of tiny fabricated usage counts. Each reaches the final verifier with one draft call and two verification calls, with no second answer-generation call; verdicts in those replays are synthetic.

The planned one-use live package tests the actual production verifier on seven controlled inputs: each recorded bad answer and its otherwise identical correction; genuinely unresolved facility applicability; an entry mixing necessary applicability with optional design details; and a question that expressly requests the drainage design. Expected verdicts and edit indices are excluded from provider input. These are verifier checks, not seven newly answered owner questions or a professional benchmark approval.

The package has a $0.24 conservative maximum within the remaining $0.250157 of the owner's $8 authorization. It verifies all 16 previous terminal result hashes, requires an exact committed-source preflight, checks the outgoing production request against its preflight hash, and uses the production provider client and spend reservations. Unknown/pending accounting stops subsequent checks. It does not read the API key during preflight or permit other network requests, answer generation, web search or manual retries.

Implementation evidence is in `permitext-sync-server/research-decision-fact-repair.mjs`, the shared policy in `research-answer-presentation.mjs`, the Research pipeline in `app.mjs`, and the three `research-decision-fact*contract.mjs` tests. The controlled inputs are in `evals/research-decision-fact-fixtures.mjs`. The full 110-question objective remains open; prior live owner-question coverage is still 35 of the additional 60.

Validation before live dispatch: the full `npm run check` passed, including the new policy/parser/repair/HTTP contracts. The parser and declared-representation restoration checks were also run separately after the final test-only addition. The seven initial verifier reservations total $0.224530, including the provider client's standard service-tier field, below the $0.24 package cap. No paid requests have been made by these local checks.

## Live outcome and fixture correction

The seven verifier-only checks completed on commit `1472f20de8696c7a783077b6683a154ebbab8776`, with seven provider calls and no pending requests. The retained result is `research-owner-live-decision-fact-verifier-2026-09-08.json`. Its permissive diagnostic flag reports five matches; this is **not five clean acceptance results**. That flag required the expected issue to appear but allowed additional issue types, including defects that prevent the fact-only repair from activating.

A subsequent exact-field comparison found that the laundry fixtures reconstructed the provider draft but omitted the production `applyResearchPlumbingSourceRepairs` step. The delivered answer had both PC 412.3 and PC 412.4 bound to its lint-strainer point; the tested draft had only PC 412.3. The verifier's citation objection to the draft was therefore valid, not a false positive against the delivered answer. Conversely, accepting that same defective binding after removing its missing facts was a missed citation defect. The four HTTP correction replays already use the complete pipeline and now assert equality of narrative, supported points and citations with the delivered answer.

The water-heater pair produced the intended failed-only-for-unnecessary-facts and corrected-pass outcomes. The applicability control inherited a supporting point that mentioned only central facilities even though its rewritten question also concerned public laundries. The requested-design control inherited an irrelevant standpipe point and asked for design outputs as prerequisites. Their failures are retained without rescoring; new controls correct those inputs.

The production verifier now receives explicit passage-to-section IDs and a per-point binding lookup. It must read every source bound to each point, and cannot treat a source elsewhere in the answer as a valid binding for that point. This is structural metadata, not a determination that a legal claim is supported. No verifier finding is suppressed and the bounded repair still rejects mixed issue types.

The new six-input collection, `research-decision-fact-fixtures-v2.mjs`, reconstructs the actually delivered answer and checks every provider-visible field against the delivery record. It contrasts that answer and its fact-only edit with a deliberately missing point binding, genuinely unresolved applicability, a mixed material/optional entry, and an expressly requested review of an existing drainage layout. The source wording for both laundry categories was rechecked against [PC 412.4](https://codelibrary.amlegal.com/codes/newyorkcity/latest/NYCadmin/0-0-0-161608). Expected outcomes remain excluded from model input. New scoring compares the complete issue-type set, deletion indices and actual pure-repair eligibility; additional defects cannot be hidden by a matching flag.

The first verifier package cost an estimated $0.01685210 including observed cache-write premiums ($0.014182 base token estimate plus $0.00267010 premium). Across 17 packages, the usage-based estimate is $3.84344505 including previously observed web-search fees, while the conservative authorization ledger is $7.789744. These are diagnostic estimates, not an invoice or account-balance reading. There have been 66 full HTTP case-turn attempts, seven verifier-only checks and 146 provider calls, all settled. The conservative remainder of the $8 authorization is $0.210256.

The corrected six-check package is capped at $0.21. Its initial production request reservations total $0.209264. As before, it requires exact committed-source preflight, hashes all prior terminal result files, locks against replay, uses the production provider client and reservations, and stops before dispatch if remaining conservative capacity is inadequate. There is no new owner-question coverage from these verifier-only controls.

Validation for the corrected package: `npm run check` passed, including the corrected fixture/scoring contract and the strengthened four-flow HTTP replay. The focused fixture test reserved all six exact production verifier bodies at no cost. No paid request was made during these checks.

## Corrected live package outcome

The six V2 checks on commit `56c5e29c38ed4a3b992c204f86fced4898426605` completed with six settled calls and three strict expectation matches. The deliberately missing lint-strainer binding was rejected; genuinely unresolved facility applicability was accepted; the mixed necessary/optional entry was rejected without deleting the material fact. However, both delivered-answer variants received additional objections to the standpipe comparison. The verifier demanded an express sentence stating that standpipes cannot displace the otherwise unqualified floor-drain requirement. Those mixed objections correctly prevented the limited fact repair; the delivered laundry correction is still not live-proven.

The layout-review control was rejected because its inherited mandatory-source checklist includes PC 412.3, while its rewritten answer retained only PC 412.4. Its actual missing-fact requests were accepted as relevant. This is a mismatch between the counterfactual answer and its retained checklist; it is not evidence that the decision-fact policy discards necessary design-review inputs. The original V2 input and failed outcome remain preserved.

V2's base usage estimate is $0.013664 plus $0.002661 in observed cache-write premiums, totaling $0.016325. Across 18 packages, the usage estimate is $3.85977005 and conservative authorization usage is $7.827715, leaving $0.172285 under the $8 authorization. There are 66 full HTTP case-turn attempts, 13 verifier-only checks and 152 provider calls, with none pending.

The next refinement explicitly accepts deductions from cited mandatory rules and supplied facts while rejecting conclusions that require unstated facts, classifications, equivalences, exceptions or outside law. It does not waive point-specific citation support. A four-check V3 package retains the exact V2 delivered-answer pair and broken-binding input, then adds an adversarial answer falsely claiming that standpipes create an exception to PC 412.4. The first three controls retain exact issue-set and repair-eligibility scoring. For the invented legal exception, multiple substantive issue labels are valid; every reported issue must belong to the specified substantive set, and an unnecessary-fact-only rejection, acceptance, or deletion indices cannot count as a match. Expected outcomes never enter provider input.

The V3 cap is $0.15 within the remaining conservative authorization, with at most four checks and eight production-client attempts. It requires a new committed-source preflight and retains the same prior-ledger hashes, no-network preflight, replay lock, accounting and dispatch guards. V2's cost test now checks its immutable historical preflight rather than asserting that future modified verifier prompts still fit a consumed package.

V3 validation: the focused four-input contract and the complete `npm run test:research-chat` suite passed, including PDF, source attribution, edition, citation, fact-repair and HTTP acceptance/rejection checks. The four initial verifier reservations total $0.141234, below $0.15. The immediately prior V2 runtime passed full `npm run check`; the subsequent V3 change is limited to the verifier instruction and diagnostic tests. All these local checks used zero paid calls.

## V3 live confirmation and current boundary

All four V3 checks matched their stated expectations on commit `dcd32ed105d30bd1480271120175911831268734`, with four settled provider calls and no pending requests. The unchanged delivered laundry answer failed only for its two unnecessary missing-fact entries, and the production pure repair was eligible. Its otherwise identical correction passed. The deliberately missing lint-strainer point binding was rejected, as was the adversarial answer inventing a standpipe exception. This supports the limited correction and the citation/application distinction on these controls; it is not an all-case accuracy score.

The complete local HTTP correction test now replays the actual provider outputs and usage from the V3 laundry checks and earlier water-heater pair. All four acceptance/rejection flows passed. Accepted flows preserve the original narrative and citations, remove only the flagged entries, perform a second verification, save the reviewed answer and charge one completed turn. Synthetic final-rejection controls prevent saving/charging. No full-answer regeneration is dispatched. These HTTP replays make no paid calls and do not constitute fresh live answer generation or a new latency sample.

V3 used an estimated $0.01127950 including cache-write premiums ($0.009439 base plus $0.00184050 premium); its conservative settled cost was $0.025769. Across 19 packages: 66 full HTTP case-turn attempts, 17 verifier-only checks, 156 provider calls and zero pending requests. The cumulative usage estimate is $3.87104955, while the conservative authorization ledger is $7.853484, leaving $0.146516 of the $8 authorization. All three decision-fact paid drivers are consumed and must not be replayed.

A current read of all 16 full-HTTP result ledgers finds 42 distinct provider-attempted IDs: 35 of the additional 60 questions, six from the original 50 (CC-01, CC-03, CC-04, DOBNOW-018, DOBNOW-019 and ZR-08), and the separate PDF-BPP probe. The 41 numbered owner cases have a latest delivered answer; the separate PDF-BPP probe's latest attempt failed. Delivery is not a source-based whole-answer grade. Within this recorded campaign, 25 additional-code questions and 44 original questions have not had a provider attempt. Prior golden-set approvals or unrelated diagnostic cohorts are not silently counted as current-runtime coverage.

The full 110-case objective remains open. Next work should prioritize remaining answer/source coverage, affirmative and threshold-boundary controls, professional review of corrected references, and representative full-service response-time/cost measurements. The layout-review counterfactual still needs a new input aligned with its mandatory-source checklist before a meaningful retest; its historical failure is not erased. No push, deployment, phone test or public-release verification occurred in this pass.
