# Research prompt compaction and broader live coverage

The previous [direct-rule confirmation](PERMITEXT_RESEARCH_DIRECT_RULE_REPAIR_2026-09-08.md) corrected two live answer defects but did not establish a speed improvement. This pass removes repeated request content and expands answer coverage. Project-workflow testing remains deferred until the owner asks.

## Exact content supplied once

Draft and verifier prompts previously repeated selected passage text under two labels. They now supply the same exact text once and identify it as the user selection through an explicit reference within that passage. Source objects, selected-text provenance, excerpts, hashes, corpus boundaries, citations and output schemas remain intact. Zoning answer obligations likewise remain complete inside the hash-checked deterministic context, with a reference replacing the second copy. No evidence was shortened, and no model, output ceiling, reasoning setting, verification policy or spending guard was changed.

Focused checks cover complete selections, partial selections, excerpts and preservation of the closing condition. The 30-case retained Zoning suite checks 60 actual draft/verifier request constructions: every complete obligation occurs once, the deterministic context is unchanged and its hash remains valid. Removing the repeated obligation line alone saves 39,102 serialized bytes across those requests, at most 2,233 in one request.

The 110-input request inspection includes both the model-specific production guard and the aggregate evaluation guard. Four currently unattempted initial drafts fit both guards at this checkpoint; draft fit is not a guarantee of complete-turn fit. Reconstructing only the two formerly duplicated lines while holding other bytes fixed makes 14 of 110 requests smaller, removes 20,747 bytes in total and at most 3,750 from one request. This is a controlled serialization comparison, not an old-runtime replay, token measurement or latency result. Authored section-ID inputs and actual whole-section Reader requests are distinguished.

Research chat, focused compaction, source/qualification regressions, both Zoning architecture suites, routing, Zoning safety and HTTP request binding are the verification scope. A general project/workflow test was not started for this pass.

## New cases and authorization

The new package attempts ZR-21 and ZR-18 once each, using their authored questions and whole-section Reader selections. They test different qualifications: restarting a discontinued nonconforming use, and using the majority district's rules on a newly assembled divided zoning lot. The controlling texts were checked against official [52-61](https://zr.planning.nyc.gov/article-v/chapter-2/52-61), [77-02](https://zr.planning.nyc.gov/article-vii/chapter-7/77-02), [77-11](https://zr.planning.nyc.gov/article-vii/chapter-7/77-11) and [77-22](https://zr.planning.nyc.gov/article-vii/chapter-7/77-22). Reference answers remain outside provider inputs.

Before dispatch, the 21-package ledger has $7.876233 conservative usage and $0.123767 remaining under the original $8 authorization, with 160 provider calls and zero pending requests. Numbered live coverage is 43/110. Each new turn is capped at $0.06 and the aggregate evaluation at $0.123767. The actual no-cost HTTP draft bounds are $0.026164 for ZR-21 and $0.025195 for ZR-18. Existing routing and deterministic verification apply; no separate model judge or manual retry is authorized by this package. Committed sources, prior ledger hashes and an independently matching HTTP preflight are required before dispatch. A failed case stops the package, and its exclusive result prevents replay.

All accounting here describes the local evaluation campaign, not a verified provider invoice or balance. No push, deployment, public eligibility change, phone test or project-workflow acceptance is included.
