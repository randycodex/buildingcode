# Permitext Zoning Research — remediation successor 3 semantic result

Date: August 30, 2026

Source branch: `codex/zoning-research-beta1`

Frozen successor commit: `f6e2fea976c79acdc451ca9cfd32c1972afc9e21`

Paid-run guard commit: `cd70aba56`

Authorization commit: `47eb20950`

Source and runner-handoff commit: `7733bfb9477f2439103a5087139290eeab5ef72c`

Dataset SHA-256: `852e521f427a418eb18c1bd45e3e764736ae50cbb09d0d0a46ce64f8cad893fc`

Run ID: `b4ef6990-5347-40d5-8654-611b893e8f1b`

## Authorization and boundary

The owner explicitly authorized exactly one run of all 30 ordered remediation-successor-3 cases, one repetition, with a maximum cumulative API spend of `$5`. The exact-SHA authorization entered a durable, non-reusable `running` state before provider dispatch and is now `consumed` by this run.

The first runner attempt, `db5d601e-3127-4ae1-af1b-dd06146d65d0`, stopped at the child authorization-state handoff before any provider dispatch. Manual review found zero matching result files and zero provider requests. The still-unspent original one-run authorization was restored only after the handoff was repaired; that recovery is retained in the authorization record. It did not create a second authorization.

Provider web support was disabled because web-tool and retrieved-content fees were not represented by the token-only evaluation ledger. The runner was configured to stop after the first execution error. The authorization did not enable the disabled 24,000-character evidence candidate, public Zoning Research, deployment, merge, push, pricing or allowance changes, professional Zoning sign-off, or customer charges. No second paid run is authorized.

## Retained result

- Status: `partial`.
- Evidence preflight: 30/30 ready before the first provider request.
- Attempted operations: three.
- Completed operations: two; both passed grading at 4.00/4.
- Failed operation: one, failed closed before grading and without a user charge.
- Unattempted operations: 27, because the stop-on-first-execution-error guard halted the run as designed.
- Provider requests: 10 settled and zero pending.
- Total paid evaluation spend: `$0.287113`, exactly matching the reconciled conservative ledger and remaining under the `$5` cap.
- Production-path requests and cost: eight requests and `$0.260801`.
- Independent grader requests and cost: two requests and `$0.026312`.
- Failed production work cost: `$0.092541`.
- Charging integrity: both completed turns were charged only to the isolated evaluation grant; the failed operation was uncharged; no customer account was involved.

Machine evidence:

- `permitext-sync-server/evals/results/2026-08-31T00-41-50-396Z-b4ef6990-5347-40d5-8654-611b893e8f1b.json` — SHA-256 `3e5728cce04fa4f810bcbb7fe8c52a7a26acdd991a2061f73055766e44a1abf9`.
- `permitext-sync-server/evals/results/2026-08-31T00-41-50-396Z-b4ef6990-5347-40d5-8654-611b893e8f1b.md` — SHA-256 `73d5f34a043ecd948bd87837a2aeea4892fa29e69aafed6063308f42aa161c56`.
- `permitext-sync-server/evals/zoning-successor-remediation-3-paid-authorization.json` — consumed-state SHA-256 `f25b5c41897c6aaa251f812e1b0565cd69d661cb2ae60886d446a9a26df26bd9`.

## Case outcomes

Passed grading:

- `zr-rules-of-construction` — 4.00/4; three production requests; `$0.085590` production cost.
- `zr-use-group-table` — 4.00/4; three production requests; `$0.082670` production cost.

Failed closed before grading:

- `zr-appendix-map-boundaries` — two production requests; `$0.092541` failed-work cost; `RESEARCH_VERIFICATION_FAILED` after the initial answer and one bounded revision both encountered `zoning_missing_mapped_location`.

The remaining 27 ordered cases were not attempted. In particular, neither of the two cases changed by remediation successor 3 reached execution, so this result does not semantically confirm those owner-approved corrections.

## Failure classification

The result does not retain a final failed answer, so it cannot prove that either generated answer was genuinely unsafe or incomplete. The frozen Appendix J case, selected evidence, required concepts, and forbidden claims are internally aligned: the answer must state the general Subarea 1 as-of-right and Subarea 2 special-permit treatment while separately refusing a parcel-specific conclusion without the applicable map and property location.

No-cost reproduction shows that the deterministic gate can reject that fully compliant structure. The current property-specific detector treats the abstract phrase `site-specific` as a live property inquiry, while its clause-local conclusion detector treats the required general statement about self-service-storage treatment in named subareas as a parcel conclusion. If the map/location boundary appears in a separate sentence, the gate reports `zoning_missing_mapped_location`; a truly unsafe statement that a particular site lies in Subarea 1 still fails. The retained failure is therefore classified as an over-broad deterministic safety trigger, not a frozen rubric/evidence defect. No case or paid result is rewritten or rescored by that diagnosis.

## Subsequent no-cost repair

Zoning safety v8 narrows only that false-positive path. A source-level regulatory summary about areas shown on named Appendix/Subarea maps may coexist with a map/location boundary in a separate sentence. The exception applies only when the complete narrative actually states the boundary and the regulatory sentence does not identify a concrete property or use a specific/deictic project subject. Direct claims such as `This site is within Subarea 1`, a general mapped-area treatment without any boundary, ordinary unresolved mapped-district grants, address-only questions, and compact-BBL questions remain fail-closed.

The exact Appendix J general-rule/separate-boundary regression now passes, while the matching no-boundary and specific-site counterexamples fail. The complete 30-case remediation-successor-3 no-cost conversation path remains 30/30 evidence-ready with public Research disabled and zero provider calls. The post-run authorization contract also now clears inherited `attemptID` and `startedAt` fields when constructing its synthetic unused authorization; the real consumed authorization remains inactive and non-reusable.

## Economics and reliability

The two completed turns cost `$0.084130` mean/p50, `$0.085298` p90, and `$0.085590` maximum. Including the failed work gives `$0.130401` per completed turn and a mechanical projection of `$13.04` per 100 all-Zoning turns. Completed-turn latency was 17.078 seconds mean/p50, 19.443 seconds p90, and 20.034 seconds maximum.

This projection is not decision-ready. The run completed only two turns against the 20-turn minimum, stopped at case three, and did not reach either changed remediation-successor-3 case. The result itself records `sampleReady: false`, `targetReady: false`, and `readyForPricingDecision: false`. The `$13.04` figure is retained only as a non-controlling partial signal. The remediation-successor-2 failed-work-amortized projection of `$20.18` per 100 all-Zoning turns and its mixed-month sensitivity remain controlling for current risk planning.

## Decision

Retain this partial result and the consumed authorization. Do not enable public Zoning Research, enable the 24,000-character candidate, change the `$20` price or 100-turn allowance, merge, push, or deploy from it. The semantic, reliability, cost, exact-release web/TestFlight physical-iPhone, and final public-release gates remain open.

The no-cost Appendix J false-positive and consumed-authorization fixture repairs are complete. This does not reconstruct or rescore the failed paid answers and does not confirm the 27 unattempted cases. Any later paid confirmation requires a new explicit exact-SHA, all-30-cases, one-repetition authorization and a new cumulative cap.
