# Permitext repaired-v2 owner-example confirmation package

Date: September 2, 2026

Status: locked; no network or model call is authorized

## Why this package exists

The owner-authorized predecessor run
`fa52cca6-d28f-4d16-968a-0d8c06d596e9` never reached a provider. All seven
first-turn attempts failed closed because the isolated runner omitted the
production-path spend-guardrail variables. It completed zero answers and spent
`$0.000000`, so it supplied no evidence about the answer corrections the owner
asked Permitext to learn.

That authorization is consumed and the failed result remains immutable. This
is a distinct package because the predecessor's permanent one-attempt rule is
not being waived or rewritten.

## Exact scope

- Frozen cohort:
  `permitext-sync-server/evals/research-product-example-cases.json`
- Cohort SHA-256:
  `39cbbe5b6d88254e585003576212f4d4227cca28f1ac4bc14c98007490f96d97`
- Seven conversations and nine ordered turns
- One repetition
- Maximum cumulative API spend: `$2.00`
- Separate paid judge requests: zero
- Official supporting web domains only: `nyc.gov`, `ny.gov`,
  `rules.cityofnewyork.us`, and `ada.gov`
- Required supporting-source seeds: the official OMH policy/regulations index
  and `ada.gov`
- Enacted Permitext corpora remain controlling for code claims
- Public Zoning Research remains disabled
- Additional-turn sales remain disabled
- No Production storage, customer data, deployment, pricing, allowance, merge,
  push, TestFlight, or release authority

## Material repair

The successor adds one bounded runtime-environment contract. Before a one-use
lock can be created, the runner now validates both layers of spend protection:

- the paid-evaluation switch, credential, versioned token prices, and exact
  `$2` cumulative evaluation cap; and
- the production-path per-request, user, and system spend guardrails used by
  the real conversation route.

The isolated values are `$1` maximum per provider request and `$2` for the
user-daily, user-monthly, system-daily, and system-monthly ceilings. The
Research kill switch is disabled only inside the isolated run, and paid
additional turns stay disabled. The runner continues to clear Production
database/storage variables and uses a temporary local store and synthetic
evaluation account.

The no-cost preflight verifies these values by calling the same validation
functions used by the live route. It also reruns the current nine-turn runtime
regression with provider credentials removed and proves zero network attempts,
zero paid model calls, and zero Production writes.

## Immutable predecessor evidence

The package binds:

- consumed predecessor authorization SHA-256
  `3b07622db1ce5061452d82006b0fe4c686761ef343022788b21fc6b15fa009d5`;
- predecessor result SHA-256
  `2b95ed0ce8b3d1a16ed1437c9d4a91fff5b8c0d32bdb34a2c74e1f40fa377f1d`;
- predecessor run ID `fa52cca6-d28f-4d16-968a-0d8c06d596e9`; and
- repair commit `dbc884a4fc6ad940359ecd49951b2b75bee044f2`.

The authorization validator recomputes all frozen implementation, fixture,
preflight, runner, and acceptance-test hashes before activation. The live
runner additionally requires the eventual package commit to be an ancestor of
the execution commit and permits only the committed authorization record to
differ inside the server tree.

## Authorization boundary

The committed record is locked: active owner-decision and scope fields are
null, `networkOrModelCallAuthorized` is false, and the permanent v2 run lock is
absent. After the package commit exists, the only accepted sentence is:

`authorize exactly package commit <40-character-package-commit> for all 9 ordered turns in 7 conversations, one repetition, with a maximum cumulative API spend of $2.`

Earlier authorizations, general instructions to continue, the consumed
predecessor authorization, and the predecessor's zero spend cannot activate
this package. A completed run will still require deterministic and owner
review; it cannot authorize public release or become an official code
determination.
