# Owner Research Zoning confirmation

The full authored-input budget inspection found ten previously unattempted Zoning questions whose initial drafts fit the remaining $0.146516 conservative authorization under the existing hybrid routing. The earlier request-size inspection covered only the additional 60 questions and therefore missed these opportunities. Initial-draft fit is not complete-turn fit or answer quality.

The new read-only inspector covers all 110 question/scenario inputs, supplied Project facts and selected passage/section IDs. It uses the production source assembly, planner, conditional-response eligibility, model routing, request builder and spend calculation. It cannot dispatch providers or retrieve web documents. The [retained full-scope request inspection](../permitext-sync-server/evals/results/research-owner-full-scope-request-envelopes-2026-09-08.json) binds all 16 source/input hashes to implementation commit `fd4b7c963166fc786b10cb85cdc9bebc525aa959`; those hashes were checked against Git. Its budget and coverage describe the 19-package campaign before the live run below. Pricing is a versioned offline fixture.

## Bounded live package

The new package contains ZR-01 (enacted text versus illustrations/tables) and ZR-14 (residential building spacing with an exception), each with its authored question and whole-section Reader selection. Reference answers stay outside provider input. Existing Luna/Terra routing and verification/repair policy remain in place. The package permits at most two case attempts and six provider requests, no manual retries and no separate model judge.

The complete pipeline is capped at $0.07 per case and $0.14 for the package, within the existing $8 owner authorization. It stops on a failed case or unsettled accounting. A persistent exclusive result file prevents paid replay. Every preceding ledger hash, the committed source and the actual HTTP preflight must match before live dispatch. No deployment or public Zoning eligibility changes are included.

The offline HTTP preflight intercepts the outgoing draft and returns a zero-usage terminal error double. It does not generate an answer or incur provider charges. The production spending guard prevents the resulting attempted Terra fallback from dispatching. The resulting first-request bounds are $0.027264 for ZR-01 and $0.028183 for ZR-14. These bounds cover drafting, not the unknown subsequent verifier or repair request.

Repeated HTTP preflights showed that isolated accounts assign fresh selected-passage IDs. The request-binding check normalizes only explicitly declared random Reader IDs while retaining their section identities and all citation occurrences. It validates the original deterministic-context hash, then binds the full context in the request hash. It also normalizes the isolated account's safety identifier. Changed source text, sections, claim bindings, instructions, model, tier or token ceilings fail comparison. The focused binding contract and repeated actual HTTP draft comparison passed at no cost.

Standard Luna/Terra rates and cache-write/long-context pricing were checked against [OpenAI pricing](https://developers.openai.com/api/docs/pricing). The existing conservative production cost calculation remains unchanged. The two governing provisions were checked against official [ZR 12-01](https://zr.planning.nyc.gov/article-i/chapter-2/12-01) and [ZR 23-371](https://zr.planning.nyc.gov/article-ii/chapter-3/23-371) before preparation.

## Live result and answer review

The [committed-source HTTP preflight](../permitext-sync-server/evals/results/research-owner-zoning-small-confirmation-preflight-2026-09-08.json) passed with 12 source hashes verified against that same commit and all 19 preceding ledger hashes preserved. The [single-use live result](../permitext-sync-server/evals/results/research-owner-live-zoning-small-confirmation-2026-09-08.json) then delivered both answers. There were exactly two provider calls, both Luna drafts, no escalation, no web calls and no pending requests. The existing direct-rule plans use deterministic verification; the recorded verification attempt is not an independent model critique.

The [complete answer review](../permitext-sync-server/evals/results/research-owner-zoning-small-answer-review-2026-09-08.json) records supported main conclusions for both cases and zero strict whole-answer passes:

- ZR-01 correctly says enacted text controls conflicting illustrations and tables, with the proper section citation. It omits the particular-over-general principle required by the existing reference key. The key and original live answer remain unchanged.
- ZR-14 correctly gives the 40-foot basic spacing rule, the 10-foot shortfall and the rear-yard-equivalent exception. It also introduces the unrequested above-125-foot branch and states its 80-foot requirement without the same paragraph's conditional reduction to 40 feet. Its “only if” exception wording is also stronger than the reference answer's scoped qualification. The actual 4,000-character Reader passage contains the upper-height exception lead but is cut off within its lot-coverage conditions. This is a source-packaging and answer-scope concern; a generic passed check does not settle it.

Local HTTP durations were 6.277 seconds and 8.751 seconds. These are two selected questions, not a controlled before/after comparison or service-wide p50/p90 estimates. The sample's usage estimate is $0.00563875 including recorded cache writes; its conservative settled cost is $0.010668. The 20-package campaign now records $7.864152 conservative usage, leaving $0.135848 of the original $8 authorization. There are 158 provider calls, 68 full HTTP case-turn attempts, 17 verifier-only checks and zero pending requests. Numbered owner-case live coverage is 43/110 attempted, leaving 67 unattempted. These accounting figures are not an invoice or a verified provider balance.

The next accuracy pass should use the retained misses to improve required-principle coverage and complete source/exception units before a fresh bounded live confirmation. Short supplemental-source fragments, broader live coverage, historical archive ingestion, and representative response-time measurements remain open. No production runtime change, push, deployment, public eligibility change or phone test occurred in this package.
