# Mapped source scope and explicitly missing facts

The retained ZR-06 storage-use draft declined a property determination but was rejected as though its general zoning rules located or approved the property. The mapped-language parser split reference-only parentheticals into apparent assertions, treated some generic use/district nouns as concrete targets, and evaluated the presence of a missing-location statement across the combined narrative rather than its individual clauses.

The local repair removes only closed reference-only parentheticals from the mapped-language analysis. Text inside parentheses that contains an assertion remains checked. It recognizes additional closed source-rule forms, explicit unprovided-location statements and equivalent `ZR §` references. Generic source descriptions remain distinct from concrete actors, and the parser preserves the immediately preceding source referent for a limited authorization clarification. This is a scope check, not substantive validation of a rule: exact source attribution and semantic verification still apply.

Each clause now contributes its own missing-location boundary, and a detected property conclusion takes priority over a general source-rule preface. Compound statements retain their actor when an elliptical continuation claims approval or authority to proceed. Dash-separated actor conclusions are checked without splitting generic appositives. The retained draft's general rules no longer fail the mapped-location check.

The draft remains unacceptable unchanged. The previous explicit-source-attribution check catches its mixed-source fifth point. The planner now also preserves special-district status and current lot area when the question explicitly lists them as unknown. The closed noun-list grammar distinguishes an earlier supplied value from a later unknown item. An answer mentioning historical lot-area changes alone does not satisfy an explicitly missing current-area requirement. These requirements are not added automatically to every property question.

The canonical replay identifies all three real defects: the wrong source binding and the two missing facts. A handwritten contrast fixes only those fields and passes the local controls and mapped-scope checks; that contrast is not a newly generated answer. The retained-answer regression appends five unsupported conclusions using six punctuation forms to the main answer, conclusion and every supporting-point heading/explanation (360 variants), plus actor/source-reference contrasts. All original Zoning safety adversarial checks remain in scope.

Validation passed: the full `npm run test:research-chat` suite and the separate Zoning safety, planner and context-excerpt contracts, with API credentials removed and provider requests intercepted. Resolving only the mapped district now leaves the other two explicitly missing facts unresolved; the context-excerpt test also verifies a subsequent question with supplied facts removes the conditional excerpt. This is not a Project workflow test.

A new single-use ZR-06 API confirmation driver is prepared. It requires a committed source state, a successful zero-API HTTP preflight, exact request/source hashes, a $0.50 batch ceiling, at most two provider requests and settled accounting. It carries forward the prior $7.289668 conservative spending within the fresh $8 round, leaving $0.710332 before this proposed confirmation. It preserves the authored question and selected sections and checks that the actual draft request carries the source-attribution instruction and all four planned missing facts. No answer-key prose is sent to the model.

All 110 numbered cases remain in the overall acceptance scope. No Project workflow, UI, phone, push or deployment work is included. A subsequent live result and cost audit must provide their own evidence; this local repair does not establish production answer quality or speed.
