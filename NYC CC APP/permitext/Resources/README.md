Bundle generated importer output here before building the app.

Expected structure:

- `nyc_code_<version>.sqlite`
- `Figures/<hash>.png`

You can keep multiple SQLite files in this folder. The app will discover them at launch and expose them in Settings.
